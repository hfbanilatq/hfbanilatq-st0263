version: '3'
services:
  drupal:
    image: drupal:8.7.8-fpm-alpine
    container_name: drupal
    restart: unless-stopped
    networks:
      - internal
      - external
    volumes:
      - /home/ec2-user/var/www/html:/var/www/html

networks:
  internal:
  external:
